import React from "react";

import {useState} from "react";

//set up a button with a counter and state for the counter
function Button() {
  const [count, setCount] = useState(0);
  return (
    <button onClick={() => setCount(count + 1)}>
      {count}
    </button>
  );
} 
export default Button;